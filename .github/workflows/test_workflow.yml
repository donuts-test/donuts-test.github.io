name: test data up
on:
  schedule: 
    - cron: "*/7 * * * *"
jobs:
  report:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      run: echo "🎉 The job was automatically triggered"
